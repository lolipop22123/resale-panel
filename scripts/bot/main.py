import asyncio
import os
from dotenv import load_dotenv

# load_dotenv() # Load environment variables from .env

async def main():
    print("Запуск Телеграм-бота...")
    # Здесь будет логика инициализации бота (например, aiogram)
    pass

if __name__ == "__main__":
    asyncio.run(main())
